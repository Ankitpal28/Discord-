version = "1.0.0"
description = "24-hour clock formatter scaffold"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial release!
        """.trimIndent(),
    )

    // Personal-use only — not submitted to the public plugin repository
    deploy.set(false)
}
