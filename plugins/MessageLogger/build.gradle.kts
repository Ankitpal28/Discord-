version = "1.0.0"
description = "Local-only cache scaffold for edited/deleted messages"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial release!
        """.trimIndent(),
    )

    // Personal-use only — not submitted to the public plugin repository
    deploy.set(false)
}
