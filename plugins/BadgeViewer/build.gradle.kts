version = "1.0.0"
description = "Adds a /whoami command showing your username and ID"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial release!
        """.trimIndent(),
    )

    // Personal-use only — not submitted to the public plugin repository
    deploy.set(false)
}
