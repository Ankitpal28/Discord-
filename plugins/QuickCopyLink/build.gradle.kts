version = "1.0.0"
description = "Adds a /copychannel command to copy a link to the current channel"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial release!
        """.trimIndent(),
    )

    // Personal-use only — not submitted to the public plugin repository
    deploy.set(false)
}
