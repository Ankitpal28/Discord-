version = "1.0.0"
description = "Force-animate avatars/emojis scaffold"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial release!
        """.trimIndent(),
    )

    // Personal-use only — not submitted to the public plugin repository
    deploy.set(false)
}
